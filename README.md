# VT Infinite

Public website for VT Infinite and Phial.

## Development

```bash
npm install
npm run dev
```

Public contact and community destinations are centralized in
`app/constants.ts`. The production build blocks accidental publication if the
approved contact email is ever replaced by a placeholder.
